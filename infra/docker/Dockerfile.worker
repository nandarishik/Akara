FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir uv==0.4.30

COPY pyproject.toml uv.lock ./
RUN uv sync --no-dev --frozen

COPY app ./app

ENV WORKER_MODE=continuous

CMD ["/app/.venv/bin/python", "-m", "app.workers.import_worker"]
